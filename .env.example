# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Together AI (chat + portrait + gallery)
TOGETHER_API_KEY=

# Together image stack (bakeoff winners)
TOGETHER_PORTRAIT_MODEL=black-forest-labs/FLUX.2-max
TOGETHER_GALLERY_MODEL=black-forest-labs/FLUX.1-kontext-max
# Optional overrides: VG_PORTRAIT_PROVIDER=together|modelslab  VG_GALLERY_PROVIDER=together|modelslab

# ModelsLab (explicit chat + fallbacks)
MODELSLAB_API_KEY=
VG_IMAGE_PROVIDER=modelslab

# Portrait fallback when Together fails
MODELSLAB_PORTRAIT_MODEL=flux-2-pro
MODELSLAB_PORTRAIT_FALLBACK_MODEL=realvisxl-v30

# Gallery fallback when Together fails (e.g. lingerie moderation)
MODELSLAB_KONTEXT_PRO_MODEL=flux-kontext-pro
MODELSLAB_KONTEXT_DEV_MODEL=flux-kontext-dev

# Explicit chat face pipeline (unchanged)
MODELSLAB_FACE_GEN_MODEL=
MODELSLAB_FACE_SWAP_MODEL=

# Browser image delivery (portrait previews + gallery)
R2_PUBLIC_BASE_URL=