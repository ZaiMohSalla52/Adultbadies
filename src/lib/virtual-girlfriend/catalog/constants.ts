/** Max auto-generated public companions in the library (raise at launch). */
export const CATALOG_COMPANION_LIMIT = 20;

/** Stable key stored on catalog rows to avoid duplicate seeds. */
export const CATALOG_PROFILE_KEY_TAG = 'catalog_profile_key';